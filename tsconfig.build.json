{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
  "include": [
    "src/**/*.ts", // Make sure all source files are included
    "src/types/**/*.d.ts" // Include the types directory for custom type declarations
  ]
}
